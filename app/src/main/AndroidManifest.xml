<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.locationaws">

    <!-- 📍 Permissões de localização -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ⚙️ (Opcional) Permissão para localização em segundo plano -->
    <!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/meulogopng"
        android:roundIcon="@mipmap/meulogopng"
        android:label="Localização GNSS"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- 🌍 Outras atividades -->
        <activity
            android:name=".FusedLocationActivity"
            android:exported="false" />

        <activity
            android:name=".GpsLocationActivity"
            android:exported="false" />

        <activity
            android:name=".GpsViewActivity"
            android:exported="false" />

        <!-- 🚀 Atividade principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
